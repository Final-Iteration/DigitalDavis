import React from "react";
import { View, Text, StyleSheet, Button } from "react-native";

const SearchedSearch = () => {
  return (
    <View>
      <Text>This is the SearchedSearch screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default SearchedSearch;
